# Recommendation engine

Pipeline: Development application contract → eligibility → scored shortlist (up to 10) → optional real model selection and ordering → structural and semantic evidence validation → server renderer → sequential growth forecasts → version-checked persistence. GET latest never invokes the model. Generation returns 0–3 items and one of READY, DATA_INCOMPLETE, NO_NEXT_GRADE, NO_ELIGIBLE_ACTIVITIES. Empty output never invents an activity.

## Rules and evidence

`multifactor-v2` is an engineering heuristic, not a learned probability. The six weighted components are normalized to [0,1]: relevant gap reduction 45%, proportion of useful effects linked to next-grade requirements 20%, role match 10%, comparable voluntary-learning history 15%, novelty 5%, explicit feedback 5%. Critical requirement gaps have weight 2 in gap reduction. Novelty remains a modest preference, so an unfamiliar minor topic does not crowd out a critical skill with recent successful learning. A separate `criticalGapReduction` component records actual critical levels closed for the quality guard and tie-breaking; it is not another score weight. Tenure has no unsupported causal weight. Duration is presented to the model and used for tie-breaking/quality trade-offs, not an invented probability. Rules selection subtracts 0.05 for each previous same-format step and each overlapping skill to encourage diversity.

History compares completed, skipped/no-show, declined and **dropped** outcomes only for voluntary activities sharing developed skills; mandatory and unrelated learning, active registrations and future records do not establish a preference. Each observation is weighted by the overlap divided by the larger skill-set size, matching format (1 versus 0.25), matching activity type (1 versus 0.5), and a 180-day recency half-life. Fit is `(weightedCompleted + 1) / (weightedCompleted + weightedUnfinished + 2)`. Evidence gives raw completed/missed/dropped counts, weighted totals, scope and half-life. `NO_RELEVANT_HISTORY` distinguishes existing but incomparable history from an entirely empty history; both are unknown/neutral 0.5. Repeated non-completion softly favors another approach without prohibiting the skill or diagnosing motivation.

The next grade and its requirements come from the role-specific catalog order. No recommendation is generated when target requirements or any required level are unknown. This conservative DATA_INCOMPLETE policy avoids silently treating missing knowledge as zero. Reaching the final grade yields NO_NEXT_GRADE. Activities with an invalid audience, prerequisites, unavailable sessions, prohibited repeats or no useful relevant gain are excluded with reasons. All effects use the development module policy. Later steps are forecast after earlier steps; caps and requirements already satisfied are respected.

Each candidate contains server-created evidence IDs: career context (`career`), concrete gap (`gap:activity:skill`) and history (`history:activity`, including explicit absence/unknown evidence). The model sees activity title, type, duration, relevant skill names and aggregate evidence, and returns only `activityId` and `evidenceIds`. Zod rejects extra fields, malformed JSON and unsupported numbers. Semantic validation rejects duplicates, outside-shortlist IDs, unknown evidence or omission of any of the three independent categories. It also rejects a first step strictly dominated by another candidate: no worse on every score dimension and duration, better on at least one. This general Pareto guard preserves real trade-offs and does not force the model to copy the heuristic order. Rejection produces labelled `LLM_DOMINATED_SELECTION` rules fallback.

Gap evidence is rebuilt from the sequential forecast, never retained from an earlier skill state. If earlier steps close all gaps originally cited by the model, the server substitutes the later activity's remaining actual gap evidence. Useless steps are removed. Arbitrary model prose is never displayed. Localized ru/kk/en templates render verified facts with readable skill names, criticality and final levels; IDs remain stable in structured data. Catalog translations fall back to English/original names; absent translations are not invented.

## Model integration and privacy

Default `LLM_PROVIDER=disabled` returns RULES_FALLBACK, `aiUsed=false`, `fallbackReason=LLM_DISABLED`. A fake transport in tests is not live AI. OpenAI integration uses HTTPS POST `/v1/responses`, `store:false`, `text.format.type=json_schema`, strict schema, no tools and no SDK retries. Refusal and incomplete output are detected. API source checked: [OpenAI Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs) and [Responses migration guide](https://developers.openai.com/api/docs/guides/migrate-to-responses).

External requests require `ALLOW_EXTERNAL_LLM=true`; an API key alone does not authorize transmission. No source dataset has been transmitted by implementation or default tests. A request contains only a shortlist, pseudonymous catalog IDs, relevant requirement facts and aggregate format history, never employee names, emails or the full employee array. Treat catalog strings as untrusted data; the system instructions prohibit following instructions embedded in them. Redirects are rejected so a loopback endpoint cannot redirect to an external service.

`LLM_PROVIDER=local` implements the native Ollama API; arbitrary OpenAI-compatible endpoints are not assumed compatible. Set `LLM_BASE_URL=http://127.0.0.1:11434` (the default local URL) and `LLM_MODEL` to an already downloaded local model. Start Ollama and download the chosen model before starting the API; model download and warm-up are not hidden in a recommendation request. The protocol is:

1. `GET {LLM_BASE_URL}/api/tags` must list the configured model.
2. `POST {LLM_BASE_URL}/api/show` with `{model,verbose:false}` must expose completion capability and model metadata. Without external permission, inventory and metadata must have no remote_host/remote_model, the model must have positive local size, GGUF format and nonempty model_info. Cloud suffixes are rejected before any request. Missing metadata fails closed before career context is sent.
3. `POST {LLM_BASE_URL}/api/chat` receives `{model,stream:false,messages,format:<JSON schema>,options:{temperature:0,num_predict:1500}}`. The adapter requires `done:true`, a complete message.content, and then independently validates its JSON and evidence. The selected response is `{"recommendations":[{"activityId":"…","evidenceIds":["career","history:…","gap:…"]}]}`. At most 3 unique activities; all references must exist in the input.

Sources checked: [Ollama chat API](https://docs.ollama.com/api/chat), [model inventory](https://docs.ollama.com/api/tags), [official metadata types](https://github.com/ollama/ollama/blob/main/api/types.go). Runtime capability checks do not assert that every local model follows the schema: invalid answers fall back to rules. Model-server responses are limited to 256 KiB. Use a trusted local Ollama process; the backend cannot attest the execution internals of a malicious server.

Without external permission the hostname must be exactly `127.0.0.1`, `[::1]` or `localhost`. Under Compose these addresses mean the API container itself. To use a separate service hostname, an operator must explicitly set the external-transfer permission after reviewing that service. The provider URL is server configuration, never a request parameter.

The default model timeout is 5 seconds, maximum configured 6 seconds; AbortSignal cancellation covers capabilities, network and response parsing. An outer 10-second deadline covers the full use case, aborts model requests and prevents subsequent writes after a delayed context read. The final database transaction timeout and queue wait fit the remaining budget. If storage cannot provide a consistent persisted response in that budget, HTTP 503 RECOMMENDATION_DEADLINE is returned; no unsaved result is presented as persisted. Database contention and deployment latency still require end-to-end measurement. Provider failure, timeout, refusal or invalid output returns an explicit rules fallback while storage remains available.

## Persistence and freshness

Context version covers profile, current skills, history, feedback, global catalog (including requirements) and availability as-of date. The cache key additionally includes employee, language, provider/model, ranking and prompt versions. TTL is 15 minutes. JSONB key ordering does not affect version equality. Profiles, history, feedback, requirements or catalog changes invalidate saved recommendations. No transaction is held while awaiting an LLM. The context is re-read afterward; any change discards model output and computes current rules. A short final PostgreSQL transaction locks version owners and checks the same versions before saving. A conflict retries once using current rules.

Snapshots retain numeric context, catalog effects and restrictions, feedback preferences and compact history without the employee name, sufficient to reproduce the ranking policy. User feedback is employee-owned, checks the set/activity relationship and increments feedbackVersion atomically. Latest feedback for an activity changes its preference component. General set feedback is stored and invalidates context without guessing which activity was disliked. A cached fallback can be reused until TTL; POST with `force:true` retries the configured provider immediately. Serialization conflicts or deadlock victims trigger one rebuild using current rules; continuous concurrent mutation returns an explicit conflict rather than stale AI.

## HR projections

HR uses one read-only Repeatable Read projection across module-owned tables, an explicit documented boundary exception. It does not invoke the model. Skills are grouped by skill, role, current grade and next grade. Unknown skill levels have their own count and never become zero. Deficit share and mean gap use only known levels with applicable requirements; applicable population and incomplete counts are separate. Participation rates divide completed records by all actual participation records within the requested window; unique participants are counted separately.

Default history window ends at the dataset `asOfDate` (or configured AS_OF_DATE) and starts 365 days earlier, both dates inclusive. Dates are not rewritten. The as-of date controls activity availability and the default reporting window; skill levels represent current application state, not a reconstructed historical skill snapshot. Use dateFrom/dateTo explicitly for current online activity outside the historical demonstration window. Signals for missing next steps, incomplete data, no recorded participation and repeated skips are observations, never motivation diagnoses or attrition predictions.

Coverage separates NOT_GENERATED, FRESH, STALE, NO_ELIGIBLE_ACTIVITIES, DATA_INCOMPLETE and NO_NEXT_GRADE. Live eligibility is computed independently of whether any recommendation was ever generated.

## Evaluation

`npm run evaluate` compares an eligible minimum-skill baseline to multi-factor rules on eleven independent synthetic fixtures using the supported `online/offline/self_paced` event formats. Cases include critical gaps with speaking skips, cold start, audience/caps, no candidate, changed history, incomplete data, dropped learning, mandatory/unrelated history, recency and mixed sequential effects. Compare only identical `stepBudget` values: one against one or up to three against up to three. All modes share eligibility and sequential gain policies. Actual selected count is reported; equal step count is not a guarantee of equal learning hours. The naive baseline's explanation metric is null rather than claiming AI explanation quality.

The explanation metric checks all three categories against **authoritative context advanced sequentially for each step**: eligibility, exact skill effects, requirements/gaps/criticality, career facts, history facts and readiness delta. It rejects internally consistent fabricated claims, such as a requirement of 99 or invented history counts, instead of trusting agreement between two fields in the same response. Its scope is structured server evidence; it does not establish that the heuristic's pedagogical assumptions are correct or evaluate free-form prose. First-choice accuracy accepts declared equal-quality alternatives, rather than treating arbitrary ID tie-breaking as AI quality. `npm run evaluate -- --output docs/evaluation-report.json` saves the report. A fixture filter limits paid tests, for example `--fixtures critical-gap-with-skips,repeated-drops,sequential-mixed-effects`.

To attempt real AI using only these synthetic fixtures, explicitly set RUN_LIVE_LLM=true and configure the provider and permission, then run evaluate. The provider is called once per nonempty fixture; its plan and original evidence IDs are used for both budget comparisons, matching production materialization. Reports distinguish `rules`, `live`, `live-not-needed` and `live-attempt-rules-fallback`, and record `liveCalls` and per-row `modelUsed`. The separate `test/evaluations/llm-live.spec.ts` is opt-in. Unit adapter tests use deterministic HTTP fakes and are labelled mocked. Rules latency is pure calculation, not HTTP/PostgreSQL. This small set cannot substantiate engagement gains or accuracy on jury profiles.
